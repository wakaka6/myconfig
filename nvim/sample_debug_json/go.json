{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Custom Golang Launch",
            "type": "delve",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "env": {},
            "args": [],
            "cwd": "${workspaceFolder}",
        }
    ]
}
